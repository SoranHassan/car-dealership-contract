# requirements.txt
# AutoGarideh - Vehicle Contract Generator
#
# Install with:  pip install -r requierments.txt
# Works on Linux, Windows and macOS (no Windows-only dependencies).

# ----------------------------------------------------------------------
# Core runtime
# ----------------------------------------------------------------------

# GUI framework
PySide6>=6.5.0

# Word (.docx) generation — cross-platform, replaces the old Windows-only
# pywin32 / MS Word COM automation.
python-docx>=1.1.0

# Persian (Jalali) date utilities
persiantools>=4.0.0

# HTTP client used by the cloud sync service
requests>=2.31.0

# Note: sqlite3 is part of the Python standard library — no install needed.

# ----------------------------------------------------------------------
# Development / tests (optional)
# ----------------------------------------------------------------------
pytest>=7.0
pytest-qt>=4.2
pytest-cov>=4.0
